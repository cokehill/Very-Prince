apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: very-prince-pgbouncer
  namespace: data
spec:
  cluster:
    name: very-prince-pg
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "2000"
      default_pool_size: "50"